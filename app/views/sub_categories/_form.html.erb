<%= form_for @subcategory, remote: true do |f| %>
	<%= f.hidden_field :id, value: @subcategory.category_id %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.submit "Add Sub-Category" %>
<% end %>