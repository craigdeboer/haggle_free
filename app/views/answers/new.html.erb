<h2>This is the question from a potential purchaser regarding your listing with the title: <%= @question.listing.title %></h2>

<h2>The question is:</h2>

<h3><%= @question.question %></h3>

<%= simple_form_for @answer, url: question_answers_path, defaults: { error: false } do |f| %>
	<%= render 'shared/error_messages', object: @answer %>
	<%= f.input :answer %>
	<%= f.submit %>
<% end %>