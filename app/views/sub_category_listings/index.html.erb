<div class="breadcrumb">
	<%= link_to "ALL CATEGORIES", root_path %>
	<span> >> </span>
	<%= link_to category.name.upcase, '#' %>
	<span> >> </span>
	<span><%= sub_category.name.upcase %></span>
</div>

<h1 class="center"><%= sub_category.name.upcase %></h1>

<%= render 'shared/listings', collection: @listings %>
