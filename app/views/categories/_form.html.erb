<%= simple_form_for @category, remote: true, defaults: { error: false } do |f| %>
	<%= f.input :name, label: "Category Name", required: false %>
	<%= f.submit "Create Category", class: "button-large" %>
<% end %>